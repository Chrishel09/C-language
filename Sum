#Print the sum of first n natural  numbers

#include<stdio.h>
int main() {
  int n;
  printf("enter a number:");
  scanf("%d",&n);

  int i=1, sum=0;
  do {
  
   sum=sum+i;
   i++;

  }
  while(i<=n);
  printf("%d",sum);
  return 0;
}

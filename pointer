#include<stdio.h>
void Max(int *a,int *b)
{
    if(*a>*b)
    printf("largest no. is %d\n",*a);
    else
printf("largest no. is %d\n",*b);
}
int main() {
    int x = 14, y=10;
    Max(&x,&y);
    return 0;
}
